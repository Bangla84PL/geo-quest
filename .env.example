# GeoQuest Environment Variables
# Copy this file to .env.local and fill in your values

# ============================================
# CESIUM.JS CONFIGURATION (Required)
# ============================================
# Get your free access token at: https://cesium.com/ion/tokens
# This token is safe to expose in the frontend (NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_CESIUM_ION_ACCESS_TOKEN=your_cesium_ion_access_token_here

# ============================================
# UPSTASH REDIS (Optional - for rate limiting)
# ============================================
# Get your free Redis database at: https://upstash.com
# If not set, rate limiting will be disabled (development mode)
UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_rest_token_here

# ============================================
# NOTES
# ============================================
# - NEXT_PUBLIC_* variables are exposed to the browser
# - All other variables are server-side only
# - Never commit .env.local to version control
# - For production deployment on Vercel, add these in the dashboard
